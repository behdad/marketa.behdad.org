# IONOS maps .py files to CGI by default. These are browser-fetched source text, never
# server-side programs; undo the inherited mapping only inside this public source directory.
RemoveHandler .py
RemoveType .py
AddType text/x-python .py
