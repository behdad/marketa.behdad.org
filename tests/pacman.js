#!/usr/bin/env node
// Focused lifecycle test for the search-only ketamine Pac-Man app. Monitor search
// stays contained in the office display, while the roaming ghost opens the same
// retained maze over its originating room. Both presentations park the sole
// simulation timer off-screen and preserve the monitor's Close/Kill semantics.
// A second load covers the static reduced-motion catch target.
"use strict";

var lib = require("./lib");

var HARNESS = [
  '<pre id="__report" style="position:fixed;left:-9999px">pending</pre>',
  '<script>',
  '(function(){',
  'function sleep(ms){return new Promise(function(r){setTimeout(r,ms);});}',
  'function key(k){document.dispatchEvent(new KeyboardEvent("keydown",{key:k,bubbles:true,cancelable:true}));}',
  'function pointer(el,type,x,y,id){return el.dispatchEvent(new PointerEvent(type,{pointerId:id||7,pointerType:"touch",isPrimary:true,button:0,buttons:type==="pointerup"?0:1,clientX:x,clientY:y,bubbles:true,cancelable:true}));}',
  'function ctxAt(el){var b=el.getBoundingClientRect(),e=new MouseEvent("contextmenu",{bubbles:true,cancelable:true,clientX:b.left+Math.min(8,b.width/2),clientY:b.top+Math.min(8,b.height/2)});return !el.dispatchEvent(e);}',
  'function menu(){return document.querySelector(".mon-ctx");}',
  'function geometry(el){var r=el.getBoundingClientRect();return {width:r.width,height:r.height,ratio:r.height?r.width/r.height:0};}',
  'var report={errors:[],steps:{}}; function S(k,v){report.steps[k]=v;}',
  'async function run(){',
  'var monitor=document.getElementById("office-monitor"), tower=document.getElementById("office-pc-desk-trio"), ghost=document.getElementById("rsvp-trip-ghost-inner"), dock=document.getElementById("monitor-dock-pacman");',
  'localStorage.removeItem("pacmanHigh");',
  'S("initial_locked",window.__pacmanUnlocked&&window.__pacmanUnlocked()===false&&!dock);',
  'if(window.__goToStage)window.__goToStage("office"); if(tower)tower.classList.add("on"); monitor.classList.add("here","screen-on","show-caps"); if(window.__monitorZoomIn)window.__monitorZoomIn(); await sleep(30);',
  '["p","a","c"].forEach(key);var lockedSearch=window.__monitorDockSearch();key("Escape");var lockedList=window.__openMonitorApp("__missing__");',
  'var initialApps=window.__chatMonitorApps();S("search_only",lockedSearch.query==="pac"&&lockedSearch.match==="pacman"&&Array.isArray(lockedList)&&lockedList.indexOf("pacman")>=0&&initialApps.some(function(app){return app.id==="pacman"&&app.access==="search";})&&!dock);',
  'window.__openMonitorApp("pacman");await sleep(60);S("search_opens",monitor.classList.contains("show-pacman")&&window.__pacmanUnlocked());window.__fitPacmanPlayer();var monitorPlayer=geometry(document.querySelector(".pac-player")),monitorBoard=geometry(document.querySelector(".pac-board"));S("monitor_player_round",Math.abs(monitorPlayer.ratio-1)<.01&&monitorBoard.ratio>1.45);window.__closeMonitorPacman();window.__resetPacmanUnlock();',
  'ghost.classList.add("roaming");ghost.dispatchEvent(new AnimationEvent("animationend",{animationName:"trip-ghost-roam",bubbles:true}));S("finished_ghost_inert",!ghost.classList.contains("roaming"));',
  'window.__goToStage("garden");var originStage=document.getElementById("stage-garden");originStage.setAttribute("data-pac-origin","kept");var monitorBefore=monitor.getAttribute("class");',
  'ghost.classList.add("roaming"); ghost.dispatchEvent(new MouseEvent("click",{bubbles:true,cancelable:true})); await sleep(120);',
  'var first=window.__pacmanState&&window.__pacmanState();',
  'S("ghost_unlocks",!!first&&first.unlocked===true&&!dock);',
  'S("stays_tileless",!document.getElementById("monitor-dock-pacman"));',
  'var presentation=window.__pacmanPresentation&&window.__pacmanPresentation();',
  'S("ghost_opens_in_room",presentation&&presentation.mode==="room"&&presentation.room==="garden"&&window.__currentStageName==="garden"&&!monitor.classList.contains("show-pacman")&&monitor.getAttribute("class")===monitorBefore&&originStage.getAttribute("data-pac-origin")==="kept");',
  'var wrap=document.getElementById("monitor-pacman-wrap");',
  'var newButton=wrap.querySelector(".pac-new"),newLabel=document.createRange();newLabel.selectNodeContents(newButton);var newButtonRect=newButton.getBoundingClientRect(),newLabelRect=newLabel.getBoundingClientRect(),newButtonStyle=getComputedStyle(newButton),newLabelDelta=(newLabelRect.top+newLabelRect.height/2)-(newButtonRect.top+newButtonRect.height/2),newMetrics={width:newButtonRect.width,height:newButtonRect.height,font:newButtonStyle.fontSize,family:newButtonStyle.fontFamily};S("new_label_centered",Math.abs(newLabelDelta)<1);',
  'window.__fitPacmanPlayer();var roomPlayer=geometry(wrap.querySelector(".pac-player")),roomBoard=geometry(wrap.querySelector(".pac-board"));S("room_player_round",Math.abs(roomPlayer.ratio-1)<.01&&roomBoard.ratio>1.45);var roomHost=document.getElementById("pacman-room-host"),roomTransform=roomHost.style.transform;roomHost.style.transform="scale(2,1)";var stretchedPlayer=geometry(wrap.querySelector(".pac-player")),stretchedBoard=geometry(wrap.querySelector(".pac-board"));window.__fitPacmanPlayer();var fittedPlayer=geometry(wrap.querySelector(".pac-player")),fittedBoard=geometry(wrap.querySelector(".pac-board"));S("nonuniform_player_fit",stretchedPlayer.ratio>1.9&&Math.abs(fittedPlayer.ratio-1)<.01&&Math.abs(stretchedBoard.ratio-fittedBoard.ratio)<.001);roomHost.style.transform=roomTransform;window.__fitPacmanPlayer();',
  'S("room_host",wrap&&wrap.parentNode&&wrap.parentNode.id==="pacman-room-host"&&document.getElementById("hunt-fullscreen-area").contains(document.getElementById("pacman-room-overlay")));',
  'var roomFrame=document.getElementById("pacman-room-frame"),roomClose=document.getElementById("pacman-room-close"),frameRect=roomFrame.getBoundingClientRect(),closeRect=roomClose.getBoundingClientRect();S("room_close_corner",roomClose.parentNode===roomFrame&&Math.abs(frameRect.right-closeRect.right-14)<1&&Math.abs(closeRect.top-frameRect.top-12)<1&&Math.abs(closeRect.width-36)<1&&Math.abs(closeRect.height-36)<1);',
  'S("board_complete",wrap&&wrap.querySelectorAll(".pac-cell").length===285&&wrap.querySelectorAll(".pac-wall").length>100&&wrap.querySelectorAll(".pac-dot,.pac-power").length>100&&wrap.querySelectorAll(".pac-ghost").length===3&&!!wrap.querySelector(".pac-actors"));',
  'var motionJumps=[];[].forEach.call(wrap.querySelectorAll(".pac-actor"),function(el){var last={r:+el.style.getPropertyValue("--ar"),c:+el.style.getPropertyValue("--ac")};new MutationObserver(function(){var next={r:+el.style.getPropertyValue("--ar"),c:+el.style.getPropertyValue("--ac")},jump=Math.max(Math.abs(next.r-last.r),Math.abs(next.c-last.c));if(jump>3&&!el.__pacMotion)motionJumps.push(jump);last=next;}).observe(el,{attributes:true,attributeFilter:["style"]});});',
  'var rules=window.__pacmanRules();S("arcade_rules",rules.playerDotMs===150&&rules.playerClearMs===133&&rules.playerFrightDotMs===135&&rules.playerFrightClearMs===118&&rules.playerPowerMs===168&&rules.ghostMs===142&&rules.frightenedMs===213&&rules.tunnelMs===266&&rules.eyesMs===107&&rules.modes.map(function(p){return p.mode;}).join(",")==="scatter,chase,scatter,chase,scatter,chase,scatter,chase");',
  'S("house_order",first.mode==="scatter"&&first.ghosts[0].name==="blinky"&&first.ghosts[0].released&&!first.ghosts[1].released&&!first.ghosts[2].released&&rules.releaseDots.join(",")==="0,18,35"&&new Set(rules.targets.map(function(t){return t.r+":"+t.c;})).size===3);',
  'var personalityBase=window.__pacmanCapture(),personality=JSON.parse(JSON.stringify(personalityBase));personality.modeIndex=1;personality.player={r:3,c:5};personality.direction=personality.desired="right";personality.ghosts[0].r=1;personality.ghosts[0].c=1;personality.ghosts[1].r=3;personality.ghosts[1].c=4;personality.ghosts[2].r=3;personality.ghosts[2].c=6;personality.ghosts.forEach(function(g){g.released=true;});window.__pacmanRestore(personality);var chaseTargets=window.__pacmanRules().targets;S("ghost_personalities",chaseTargets[0].r===3&&chaseTargets[0].c===5&&chaseTargets[1].r===5&&chaseTargets[1].c===13&&chaseTargets[2].r===17&&chaseTargets[2].c===-3);window.__pacmanRestore(personalityBase);',
  'var focused=true;try{Object.defineProperty(document,"hasFocus",{configurable:true,value:function(){return focused;}});}catch(e){}',
  'key("ArrowUp");await sleep(220);S("blocked_start_waits",window.__pacmanState().status==="ready"&&window.__pacmanState().score===0&&!window.__pacmanState().running);',
  'key("ArrowLeft");await sleep(190);var visualLine=parseInt(wrap.querySelector(".pac-player").style.getPropertyValue("--ac"),10),smoothState=window.__pacmanState();S("classic_smooth_move",smoothState.score>=10&&visualLine>=97&&visualLine<=109&&getComputedStyle(wrap.querySelector(".pac-player")).transform==="none");for(var repeat=0;repeat<16;repeat++){await sleep(25);key("ArrowLeft");}await sleep(180);',
  'var moved=window.__pacmanState(); S("held_key_plays",moved.status==="running"&&moved.score>=20&&moved.remaining<first.remaining&&moved.running);S("independent_motion",motionJumps.length===0);',
  'for(var motionWait=0;motionWait<20&&![].some.call(wrap.querySelectorAll(".pac-actor"),function(el){return !!el.__pacMotion;});motionWait++)await sleep(5);var actors=[].slice.call(wrap.querySelectorAll(".pac-actor:not(.pac-wrap-copy)")),beforePause=actors.map(function(el){return el.style.getPropertyValue("--ar")+":"+el.style.getPropertyValue("--ac");});var spaceEvent=new KeyboardEvent("keydown",{key:" ",code:"Space",bubbles:true,cancelable:true});var spacePrevented=!document.dispatchEvent(spaceEvent),pausedNow=window.__pacmanState(),afterPause=actors.map(function(el){return el.style.getPropertyValue("--ar")+":"+el.style.getPropertyValue("--ac");});await sleep(180);var pausedStill=window.__pacmanState();S("space_pauses",spacePrevented&&pausedNow.paused&&!pausedNow.running&&pausedStill.score===pausedNow.score&&typeof window.__pacmanTogglePause==="function");S("pause_motion_settles",beforePause.join(",")===afterPause.join(","));key(" ");await sleep(80);S("space_resumes",!window.__pacmanState().paused&&window.__pacmanState().running);',
  'await sleep(1200);var opening=window.__pacmanState();S("opening_survival",opening.lives===3&&opening.mode==="scatter"&&opening.ghosts[0].released&&!opening.ghosts[2].released);',
  'focused=false;window.dispatchEvent(new Event("blur"));var paused=window.__pacmanState();await sleep(260);S("blur_parks",!paused.running&&window.__pacmanState().score===paused.score);',
  'focused=true;window.dispatchEvent(new Event("focus"));await sleep(210);S("focus_resumes",window.__pacmanState().running);',
  'var saved=window.__pacmanCapture(),pelletMask=new Array(saved.pellets.length).fill(false);[].forEach.call(wrap.querySelectorAll(".pac-dot,.pac-power"),function(el){var r=parseInt(el.style.getPropertyValue("--r"),10)-1,c=parseInt(el.style.getPropertyValue("--c"),10)-1;pelletMask[r*19+c]=true;});var saveHits=0,realSave=window.__saveLoftCheckpoint;window.__saveLoftCheckpoint=function(){saveHits++;return true;};',
  'async function proveTunnel(c,dir,keyName){var tunnel=JSON.parse(JSON.stringify(saved));tunnel.status="ready";tunnel.player={r:7,c:c};tunnel.direction=tunnel.desired=dir;tunnel.playerAte=false;tunnel.ghosts.forEach(function(g,i){g.r=1;g.c=1+i*8;g.released=false;g.releaseAt=999;});window.__pacmanRestore(tunnel);key(keyName);await sleep(210);var player=wrap.querySelector(".pac-player:not(.pac-wrap-copy)"),copy=wrap.querySelector(".pac-player.pac-wrap-copy"),pc=+player.style.getPropertyValue("--ac"),cc=copy&&+copy.style.getPropertyValue("--ac");return !!copy&&(dir==="left"?pc<=1&&cc>=217:pc>=217&&cc<=1);}',
  'var tunnelLeft=await proveTunnel(0,"left","ArrowLeft");window.__pacmanRestore(saved);var tunnelRight=await proveTunnel(18,"right","ArrowRight");focused=false;window.dispatchEvent(new Event("blur"));var tunnelParked=!wrap.querySelector(".pac-wrap-copy")&&!window.__pacmanState().running;focused=true;window.dispatchEvent(new Event("focus"));window.__pacmanRestore(saved);S("tunnel_motion",tunnelLeft&&tunnelRight);S("tunnel_cleanup",tunnelParked);',
  'var collision=JSON.parse(JSON.stringify(saved));collision.status="ready";collision.score=0;collision.lives=3;collision.fright=0;collision.modeIndex=1;collision.player={r:13,c:9};collision.direction=collision.desired="left";collision.ghosts[0]={r:13,c:8,dir:"right"};collision.ghosts[1]={r:1,c:1,dir:"right"};collision.ghosts[2]={r:1,c:17,dir:"left"};collision.pellets=pelletMask.slice();window.__pacmanRestore(collision);key("ArrowLeft");await sleep(280);var hit=window.__pacmanState();S("collision_costs_life",hit.status==="ready"&&hit.lives===2&&!hit.running&&saveHits>0);',
  'var power=JSON.parse(JSON.stringify(collision));power.player={r:13,c:2};power.ghosts[0]={r:13,c:1,dir:"right"};power.lives=3;power.status="ready";power.score=0;power.fright=0;window.__pacmanRestore(power);key("ArrowLeft");await sleep(280);var ate=window.__pacmanState(),ateSaved=window.__pacmanCapture();S("power_eats_ghost",ate.status==="running"&&ate.lives===3&&ate.score===250&&ate.high===250&&ateSaved.fright>0);',
  'var over=JSON.parse(JSON.stringify(collision));over.lives=1;window.__pacmanRestore(over);key("ArrowLeft");await sleep(280);var lost=window.__pacmanState(),overSaved=window.__pacmanCapture();S("collision_game_over",lost.status==="over"&&lost.lives===0&&!lost.running);var againRange=document.createRange();againRange.selectNodeContents(newButton);var againRect=newButton.getBoundingClientRect(),againTextRect=againRange.getBoundingClientRect(),againStyle=getComputedStyle(newButton),againParent=newButton.parentElement.getBoundingClientRect();S("play_again_fit",newButton.classList.contains("pac-again")&&newButton.textContent==="PLAY AGAIN"&&againRect.width<newMetrics.width*.75&&againRect.width>againTextRect.width&&Math.abs(againRect.height-newMetrics.height)<.1&&againStyle.fontSize===newMetrics.font&&againStyle.fontFamily===newMetrics.family&&Math.abs(parseFloat(againStyle.paddingLeft)-parseFloat(againStyle.fontSize))<.05&&Math.abs(againRect.left+againRect.width/2-(againParent.left+againParent.width/2))<1);window.__setLang("cs");await sleep(20);var againCsRect=newButton.getBoundingClientRect();S("play_again_cs_fit",newButton.textContent==="HRÁT ZNOVU"&&againCsRect.width<newMetrics.width*.75&&againCsRect.width<=againParent.width&&Math.abs(againCsRect.height-newMetrics.height)<.1);window.__setLang("en");window.__pacmanRestore(overSaved);S("game_over_roundtrip",window.__pacmanState().status==="over"&&window.__pacmanState().lives===0);',
  'var win=JSON.parse(JSON.stringify(collision)),lastIdx=13*19+8;win.ghosts[0]={r:1,c:1,dir:"right"};win.pellets=win.pellets.map(function(_,i){return i===lastIdx;});window.__pacmanRestore(win);key("ArrowLeft");await sleep(180);var cleared=window.__pacmanState();S("maze_clears",cleared.status==="win"&&cleared.remaining===0&&!cleared.running);',
  'wrap.querySelector(".pac-new").click();var fresh=window.__pacmanState(),freshButtonRect=newButton.getBoundingClientRect();S("new_resets",fresh.status==="ready"&&fresh.score===0&&fresh.high===250&&fresh.lives===3&&fresh.remaining===143&&!newButton.classList.contains("pac-again")&&Math.abs(freshButtonRect.width-newMetrics.width)<.1);',
  'var controls=wrap.querySelector(".pac-controls");S("desktop_pad_hidden",getComputedStyle(controls).display==="none");controls.style.display="grid";var cb=controls.getBoundingClientRect(),cx=cb.left+cb.width/2,cy=cb.top+cb.height/2,threshold=Math.max(3,Math.min(7,Math.min(cb.width,cb.height)*.16));pointer(controls,"pointerdown",cx,cy);pointer(controls,"pointermove",cx+threshold+1,cy);var dragRight=window.__pacmanCapture().desired;var anchorX=cx+threshold+1;pointer(controls,"pointermove",anchorX-threshold*1.2,cy);var dragHeld=window.__pacmanCapture().desired;var leftX=anchorX-threshold*1.7;pointer(controls,"pointermove",leftX,cy);var dragLeft=window.__pacmanCapture().desired;pointer(controls,"pointermove",leftX,cy+threshold+1);var dragDown=window.__pacmanCapture().desired;pointer(controls,"pointerup",leftX,cy+threshold+1);S("live_drag_turns",dragRight==="right"&&dragHeld==="right"&&dragLeft==="left"&&dragDown==="down"&&!controls.classList.contains("dragging"));',
  'wrap.querySelector(".pac-new").click();wrap.querySelector(".pac-btn[data-dir=left]").click();S("pad_plays",window.__pacmanState().status==="running"&&window.__pacmanState().running);',
  'window.__saveLoftCheckpoint=realSave;window.__pacmanRestore(saved);var fullArea=document.getElementById("hunt-fullscreen-area");fullArea.classList.add("is-fullscreen");window.__goToStage("office");key("Backspace");await sleep(80);',
  'S("close_stops",document.getElementById("pacman-room-overlay").hidden&&window.__pacmanPresentation().mode===null&&window.__currentStageName==="garden"&&!monitor.classList.contains("show-pacman")&&wrap.parentNode&&wrap.parentNode.tagName.toLowerCase()==="foreignobject"&&window.__pacmanState().running===false&&originStage.getAttribute("data-pac-origin")==="kept"&&fullArea.classList.contains("is-fullscreen"));fullArea.classList.remove("is-fullscreen");',
  'window.__openRoomPacman();document.getElementById("pacman-room-close").click();var dismissClosed=document.getElementById("pacman-room-overlay").hidden&&window.__currentStageName==="garden";window.__openRoomPacman();key("Escape");var escapeClosed=document.getElementById("pacman-room-overlay").hidden&&window.__currentStageName==="garden";S("room_dismiss_paths",dismissClosed&&escapeClosed);',
  'window.__pacmanRestore(saved); var restored=window.__pacmanState(); S("checkpoint_roundtrip",restored.unlocked===true&&restored.score===saved.score&&restored.remaining===saved.pellets.filter(Boolean).length&&restored.running===false);',
  'window.__pacmanRestore(saved);',
  'monitor.classList.add("show-caps");window.__unlockPacman(true);await sleep(60);S("monitor_reopen_contained",window.__currentStageName==="office"&&window.__pacmanPresentation().mode==="monitor"&&monitor.classList.contains("show-pacman"));var inAppPrevented=ctxAt(wrap),killBtn=menu()&&menu().querySelector(".ctx-kill");S("app_context",inAppPrevented&&!!killBtn&&!menu().querySelector(".ctx-restart"));if(killBtn)killBtn.click();',
  'focused=false;window.dispatchEvent(new Event("blur"));focused=true;window.dispatchEvent(new Event("focus"));await sleep(260);var dying=window.__pacmanState();S("kill_stays_frozen",dying.dying===true&&dying.running===false);await sleep(2200);',
  'var killed=window.__pacmanState(); S("kill_resets_not_relocks",killed.unlocked===true&&killed.status==="ready"&&killed.score===0&&!killed.dying&&!monitor.classList.contains("show-pacman"));',
  'window.__resetPacmanUnlock(); S("full_reset_relocks",window.__pacmanUnlocked()===false&&!document.getElementById("monitor-dock-pacman")&&window.__pacmanState().running===false&&window.__pacmanState().high===250);',
  '}',
  'window.addEventListener("load",function(){setTimeout(function(){run().catch(function(e){window.__errs.push("harness:"+String(e&&e.stack||e));}).then(function(){report.errors=window.__errs;document.getElementById("__report").textContent=JSON.stringify(report);});},400);});',
  '})();',
  '</script>'
].join("\n");

var failures=0;
function pass(msg){console.log("  ✓ "+msg);}
function fail(msg,detail){failures++;console.log("  ✗ "+msg);if(detail)console.log("      "+String(detail).split("\n").join("\n      "));}

console.log("rsvp.html ketamine-ghost Pac-Man:");
var r=lib.runPageSync("rsvp.html",HARNESS,10000,{patchRaf:true,forceMotion:true});
if(!r) fail("harness reported");
else {
  var checks={initial_locked:"app begins undiscovered and has no desktop tile",search_only:"Pac-Man is searchable and listed without receiving a desktop tile",search_opens:"monitor search launches Pac-Man inside the office display",monitor_player_round:"monitor Pac-Man renders round while its maze stays wide",finished_ghost_inert:"the finished roaming ghost loses its invisible hit target",ghost_unlocks:"clicking the ketamine ghost reveals Pac-Man without adding a tile",stays_tileless:"Pac-Man stays off the desktop after discovery",ghost_opens_in_room:"ghost click opens Hack-Man over its originating room without changing monitor state",new_label_centered:"New Game is vertically centered in its control",play_again_fit:"Play Again uses compact proportional side padding without changing height or type",play_again_cs_fit:"the compact Czech Play Again label fits at the same height",room_player_round:"room Pac-Man renders round while its maze stays wide",nonuniform_player_fit:"non-uniform presentation scaling is corrected on Pac-Man without resizing the maze",room_host:"the room presentation reuses the live board inside the whole-loft fullscreen subtree",room_close_corner:"the room Dismiss control sits on the Hack-Man HUD corner",board_complete:"maze, pellets, player, ghosts, and the subcell actor grid render",arcade_rules:"player, normal ghost, frightened, tunnel, eyes, and mode timings match level one",house_order:"Blinky starts out while Inky and Clyde wait in order with distinct scatter targets",ghost_personalities:"Blinky, Inky, and Clyde calculate their distinct chase targets",blocked_start_waits:"a blocked opening direction does not start the ghosts",classic_smooth_move:"level-one movement advances at dot-eating pace and visibly interpolates without transforms",held_key_plays:"held arrow input cannot starve the simulation timer",independent_motion:"one actor tick cannot snap another actor's in-flight interpolation",space_pauses:"Space parks active Pac-Man before the global music shortcut",pause_motion_settles:"pausing lets the current visual segment settle instead of snapping",space_resumes:"Space resumes the same round",opening_survival:"the opening scatter and staggered releases prevent an immediate three-ghost pile-on",blur_parks:"blur parks the simulation without advancing state",focus_resumes:"focus resumes an open running maze",tunnel_motion:"both tunnel directions cross the clipped edges with paired visuals",tunnel_cleanup:"blur removes transient tunnel copies while parking the maze",collision_costs_life:"a collision costs one life, checkpoints, and waits for input",power_eats_ghost:"a corner power pellet makes a collided ghost edible and banks the high score",collision_game_over:"the last collision reaches game over with zero lives",game_over_roundtrip:"game-over checkpoints retain zero lives",maze_clears:"the final pellet ends the maze in a win",new_resets:"New restores the maze but retains the high score",desktop_pad_hidden:"desktop Hack-Man hides the touch direction pad",live_drag_turns:"a held direction-pad drag queues repeated turns with reversal hysteresis",pad_plays:"the on-screen direction pad starts the game",close_stops:"Backspace parks the maze, restores its monitor host and origin room, and leaves whole-loft fullscreen intact",room_dismiss_paths:"the room Dismiss control and Escape both close without moving the player",checkpoint_roundtrip:"checkpoint retains discovery without exposing a desktop tile",monitor_reopen_contained:"a later monitor launch still navigates to and stays inside the office display",app_context:"the open game exposes Kill without Restart",kill_stays_frozen:"focus changes cannot restart a Pac-Man Kill flash",kill_resets_not_relocks:"Kill resets the maze but keeps the discovery",full_reset_relocks:"full reset hides Pac-Man again without erasing the high score"};
  Object.keys(checks).forEach(function(k){if(r.steps[k])pass(checks[k]);else fail(checks[k],JSON.stringify(r.steps));});
  if(r.errors.length===0)pass("no uncaught JS errors");else fail("no uncaught JS errors",r.errors.slice(0,12).join("\n"));
}

var REDUCED = [
  '<pre id="__report" style="position:fixed;left:-9999px">pending</pre>',
  '<script>window.addEventListener("load",function(){setTimeout(function(){',
  'var ghost=document.getElementById("rsvp-trip-ghost-inner");ghost.classList.add("roaming");var css=getComputedStyle(ghost);',
  'document.getElementById("__report").textContent=JSON.stringify({errors:window.__errs,steps:{static_target:css.animationName==="none"&&parseFloat(css.opacity)>.9&&css.pointerEvents!=="none"}});',
  '},350);});</script>'
].join("\n");
var rm=lib.runPageSync("rsvp.html",REDUCED,1200,{patchRaf:true,forceReduce:true,chromeFlags:"--force-prefers-reduced-motion=reduce"});
if(rm&&rm.steps.static_target)pass("reduced motion presents a static visible catch target");else fail("reduced motion presents a static visible catch target",rm&&JSON.stringify(rm.steps));
if(rm&&rm.errors.length===0)pass("reduced-motion load has no uncaught JS errors");else fail("reduced-motion load has no uncaught JS errors",rm&&rm.errors.join("\n"));

var COARSE = [
  '<pre id="__report" style="position:fixed;left:-9999px">pending</pre>',
  '<script>window.addEventListener("load",function(){setTimeout(function(){',
  'window.__unlockPacman(false);window.__openRoomPacman();window.__fitPacmanPlayer();var controls=document.querySelector(".pac-controls"),player=document.querySelector(".pac-player").getBoundingClientRect(),board=document.querySelector(".pac-board").getBoundingClientRect();',
  'document.getElementById("__report").textContent=JSON.stringify({errors:window.__errs,steps:{coarse:matchMedia("(any-pointer:coarse)").matches,pad:getComputedStyle(controls).display,playerRatio:player.width/player.height,boardRatio:board.width/board.height}});',
  '},350);});</script>'
].join("\n");
var coarse=lib.runPageSync("rsvp.html",COARSE,1400,{patchRaf:true,forceMotion:true,chromeFlags:"--blink-settings=primaryPointerType=2,availablePointerTypes=2,primaryHoverType=0,availableHoverTypes=0"});
if(coarse&&coarse.steps.coarse&&coarse.steps.pad==="grid")pass("touch/coarse-pointer Hack-Man retains its direction pad");else fail("touch/coarse-pointer Hack-Man retains its direction pad",coarse&&JSON.stringify(coarse.steps));
if(coarse&&Math.abs(coarse.steps.playerRatio-1)<.01&&coarse.steps.boardRatio>1.45)pass("touch/coarse-pointer Pac-Man renders round while its maze stays wide");else fail("touch/coarse-pointer Pac-Man renders round while its maze stays wide",coarse&&JSON.stringify(coarse.steps));
if(coarse&&coarse.errors.length===0)pass("coarse-pointer load has no uncaught JS errors");else fail("coarse-pointer load has no uncaught JS errors",coarse&&coarse.errors.join("\n"));

console.log("");
if(failures){console.log(failures+" check(s) failed.");process.exit(1);} console.log("All checks passed.");
